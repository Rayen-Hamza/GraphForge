{
  "buildCommand": "npm run build -- --configuration production",
  "outputDirectory": "dist/ui/browser",
  "framework": null,
  "rewrites": [
    { "source": "/api/v1/:path*", "destination": "${API_URL}/api/v1/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
